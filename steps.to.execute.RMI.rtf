
1. Compile to get *.class files:

  	  java -d destination_directory *.java

2. Go to the location of .class files and start RMI registry:
	 
   	 rmiregistry

3. In a new terminal window run Server program

	java example.hello.Server

4. In a new terminal window run Client program

	java example.hello.Client

** Note: rmiregistry, server and client must be run in different terminal windows
